import { extendTheme } from '@chakra-ui/react';

const theme = {
   fonts: {
       custom: '"Segue UI", sans-serif',
   },
    colors:{
        ROCKWOOL:{
            100: "#FFFFFF",
            500: "#c6000e",
            900: "#c6000e"
        }
    }}

export default extendTheme(theme);